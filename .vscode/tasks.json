{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clear Database",
            "type": "shell",
            "command": "rm -f ${workspaceFolder}/mealie-auto-tagger/src/database.db.sqlite3",
            "problemMatcher": [],
        },
        {
            "label": "lint",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/mealie-auto-tagger"
            },
            "command": "poetry run pylint --recursive=y ./src/mealie_auto_tagger/ ./tests/",
            "problemMatcher": [],
        }
    ]
}
